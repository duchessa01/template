---
// src/components/Header.astro

// ========================================
// NAVIGAZIONE PRINCIPALE
// ========================================

const mainNavigation = [
  {
    name: 'Team',
    mobileName: 'Il Team',
    href: '/team'
  },
  {
    name: 'Sedi',
    mobileName: 'Sedi',
    href: '/sedi'
  },
  {
    name: 'Prima & Dopo',
    mobileName: 'Prima & Dopo',
    href: '/prima-e-dopo'
  },
  {
    name: 'Contatti',
    mobileName: 'Contatti',
    href: '/contatti'
  },
];


// ========================================
// VOCE TRATTAMENTI
// ========================================

const treatmentsNavigation = {
  name: 'Trattamenti',
  href: '/trattamenti',
};

// ========================================
// SOTTOCATEGORIE TRATTAMENTI
// ========================================

const treatmentCategories = [
  {
    title: 'Medicina Estetica Viso',
    items: [
      {
        name: 'Filler Acido Ialuronico',
        href: '/trattamenti/viso#filler'
      },
      {
        name: 'Botox / Botulino',
        href: '/trattamenti/viso#botox'
      },
      {
        name: 'Biostimolazione & Polinucleotidi',
        href: '/trattamenti/viso#biostimolazione'
      },
      {
        name: 'Rinofiller (Rinoplastica Non Chirurgica)',
        href: '/trattamenti/viso#rinofiller'
      },
      {
        name: 'Peeling Medico Avanzato',
        href: '/trattamenti/viso#peeling'
      },
    ]
  },

  {
    title: 'Rimozione Inestetismi & Laser',
    items: [
      {
        name: 'Laser CO2 Frazionato (Resurfacing)',
        href: '/trattamenti/laser#co2'
      },
      {
        name: 'Epilazione Medicale Definitiva',
        href: '/trattamenti/laser#epilazione'
      },
      {
        name: 'Rimozione Macchie & Pigmentazioni',
        href: '/trattamenti/laser#macchie'
      },
      {
        name: 'Trattamento Cicatrici Acneiche',
        href: '/trattamenti/laser#acne'
      },
    ]
  },

  {
    title: 'Corpo & Body Contouring',
    items: [
      {
        name: 'Mesoterapia Anti-Cellulite',
        href: '/trattamenti/corpo#mesoterapia'
      },
      {
        name: 'Radiofrequenza Medicale Corpo',
        href: '/trattamenti/corpo#radiofrequenza'
      },
      {
        name: 'Adipocitoclasia / Adipe Localizzato',
        href: '/trattamenti/corpo#adipe'
      },
      {
        name: 'Tonificazione Muscolare Intensa',
        href: '/trattamenti/corpo#tonificazione'
      },
    ]
  },

  {
    title: 'Chirurgia & Rigenerativa',
    items: [
      {
        name: 'Medicina Rigenerativa (PRP)',
        href: '/trattamenti/viso#rigenerativa'
      },
      {
        name: 'Consulenza Chirurgia Plastica',
        href: '/contatti'
      },
      {
        name: 'Check-up Cutaneo Diagnostico',
        href: '/contatti'
      },
    ]
  }
];

---

<header
  id="site-header"
  class="fixed top-0 left-0 right-0 z-40 bg-white text-[#111111] border-b border-neutral-200"
>
  <div class="max-w-7xl mx-auto px-6 h-20 flex items-center justify-between">

    <!-- Logo Brand -->
    <a href="/" class="flex flex-col group">
      <span
        id="logo-title"
        class="text-xl md:text-2xl font-black tracking-tighter text-[#111111] uppercase"
      >
        OG MEDICA
      </span>

      <span
        id="logo-subtitle"
        class="text-[11px] font-semibold tracking-widest uppercase text-neutral-400 -mt-1"
      >
        Medicina Estetica Avanzata
      </span>
    </a>

    <!-- Navigation Desktop -->
    <nav class="hidden lg:flex items-center space-x-8 text-xs font-semibold uppercase tracking-widest">
      

      <!-- MEGA MENU DROPDOWN -->
      <div class="group py-7">

        <a
  href={treatmentsNavigation.href}
  class="relative nav-item flex items-center gap-1 text-[#111111] hover:opacity-60 focus:outline-none uppercase tracking-widest font-semibold cursor-pointer"
>
  {treatmentsNavigation.name}

  <svg
    class="w-3.5 h-3.5 text-neutral-500 group-hover:rotate-180 transition-transform duration-200"
    fill="none"
    stroke="currentColor"
    viewBox="0 0 24 24"
  >
    <path
      stroke-linecap="round"
      stroke-linejoin="round"
      stroke-width="2"
      d="M19 9l-7 7-7-7"
    />
  </svg>
</a>


        <!-- Dropdown Panel -->
        <div
          class="absolute left-1/2 -translate-x-1/2 top-full w-screen max-w-6xl bg-white border border-neutral-100 shadow-xl rounded-2xl p-8 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 grid grid-cols-4 gap-8 pointer-events-none group-hover:pointer-events-auto text-[#111111]"
        >

          {treatmentCategories.map((cat) => (
            <div>
              <h5
                class="text-[12px] font-bold text-[#111111] uppercase tracking-wider mb-4 border-b border-neutral-100 pb-2"
              >
                {cat.title}
              </h5>

              <ul class="space-y-2.5">
                {cat.items.map((item) => (
                  <li>
                    <a
                      href={item.href}
                      class="text-[11px] font-normal text-neutral-500 hover:text-[#111111] hover:translate-x-1 transition-all inline-block capitalize"
                    >
                      {item.name}
                    </a>
                  </li>
                ))}
              </ul>
            </div>
          ))}

          <div
            class="col-span-4 bg-neutral-900 p-4 rounded-xl flex items-center justify-between mt-2 border border-neutral-100"
          >
            <span class="text-xs text-white">
              Non sai qual è il trattamento più adatto al tuo inestetismo?
            </span>

            <a
              href="/trattamenti"
              class="text-xs font-bold text-white uppercase tracking-wider hover:underline"
            >
              Vedi l'Hub Completo dei Trattamenti &rarr;
            </a>
          </div>

        </div>
      </div>
      
      {mainNavigation.map((item) => (
  <a
    href={item.href}
    class="nav-item text-[#111111] hover:opacity-60 py-2"
  >
    {item.name}
  </a>
))}

    </nav>

    <!-- Right Header CTA -->
    <div class="hidden lg:flex items-center space-x-4">
      <a
        href="/contatti"
        target="_blank"
        rel="noopener noreferrer"
        id="cta-btn"
        class="bg-[#4CAF50] text-white text-[11px] font-semibold uppercase tracking-widest px-6 py-3 rounded-full hover:bg-neutral-800 transition-colors shadow-sm"
      >
        Prenota una visita
      </a>
    </div>

    <!-- Mobile Menu Toggle -->
    <button
      id="mobile-menu-btn"
      class="lg:hidden p-2 text-[#111111] focus:outline-none"
      aria-label="Apri Menu"
    >
      <svg
        class="w-6 h-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M4 6h16M4 12h16M4 18h16"
        />
      </svg>
    </button>

  </div>
</header>

<!-- MOBILE DRAWER -->
<div
  id="mobile-menu"
  class="fixed inset-0 bg-neutral-950/70 backdrop-blur-md z-[999] hidden opacity-0 transition-opacity duration-300 text-[#111111]"
>
  <div
    class="fixed right-0 top-0 bottom-0 w-full max-w-xs bg-white p-6 overflow-y-auto flex flex-col justify-between shadow-2xl"
  >

    <div>

      <div class="flex items-center justify-between pb-6 border-b border-neutral-100">

        <span class="text-lg font-bold uppercase tracking-tight text-[#111111]">
          Menu
        </span>

        <button
          id="close-menu-btn"
          class="p-2 text-neutral-500 hover:text-black focus:outline-none"
          aria-label="Chiudi Menu"
        >
          <svg
            class="w-6 h-6"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M6 18L18 6M6 6l12 12"
            />
          </svg>
        </button>

      </div>

      <nav class="mt-6 space-y-2 font-semibold uppercase tracking-widest">

        <div>

          

          <div class="border-b border-neutral-50">

  <button
  id="mobile-treatments-btn"
  type="button"
  class="w-full flex items-center justify-between text-[#111111] font-bold text-left uppercase cursor-pointer"
  aria-expanded="false"
>
<!-- MENU "TRATTAMENTI" MOBILE -->
  <span class="block py-2 text-[#111111] font-semibold border-b border-neutral-100">
    {treatmentsNavigation.name}
  </span>

  <svg
    id="mobile-treatments-icon"
    class="w-4 h-4 text-neutral-500 transition-transform duration-200"
    fill="none"
    stroke="currentColor"
    viewBox="0 0 24 24"
  >
    <path
      stroke-linecap="round"
      stroke-linejoin="round"
      stroke-width="2"
      d="M6 9l6 6 6-6"
    />
  </svg>
</button>


  

  <!-- Tutte le categorie + tutti i trattamenti -->
  <div
    id="mobile-treatments-submenu"
    class="hidden pl-3 pt-1 space-y-4"
  >
  
    {treatmentCategories.map((cat) => (
      <div>

        <!-- Macro categoria -->
        <h4 class="text-[12px] font-bold text-[#111111] uppercase tracking-wider mb-2">
          {cat.title}
        </h4>

        <!-- Singoli trattamenti -->
        <div class="space-y-2 pl-2 border-l border-neutral-100">

          {cat.items.map((item) => (
            <a
              href={item.href}
              class="block text-[11px] font-normal normal-case tracking-normal text-neutral-500 hover:text-[#111111]"
            >
              {item.name}
            </a>
          ))}

        </div>
      </div>
    ))}
    <a
    href={treatmentsNavigation.href}
    class="block py-2 text-[#111111] text-xs font-bold border-b border-neutral-50 cursor-pointer"
    >
    Tutti i Trattamenti
    </a>

  </div>

</div>

        </div>

        {mainNavigation.map((item) => (
  <a
    href={item.href}
    class="block py-2 text-[#111111] border-b border-neutral-100"
  >
    {item.name}
  </a>
))}

      </nav>

    </div>

  </div>
</div>

<script>
  // Gestione Drawer Mobile
  const menuBtn = document.getElementById('mobile-menu-btn');
  const closeBtn = document.getElementById('close-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const mobileTreatmentsBtn = document.getElementById('mobile-treatments-btn');
const mobileTreatmentsSubmenu = document.getElementById('mobile-treatments-submenu');
const mobileTreatmentsIcon = document.getElementById('mobile-treatments-icon');

mobileTreatmentsBtn?.addEventListener('click', () => {
  const isOpen = !mobileTreatmentsSubmenu?.classList.contains('hidden');

  if (mobileTreatmentsSubmenu) {
    mobileTreatmentsSubmenu.classList.toggle('hidden', isOpen);
  }

  mobileTreatmentsIcon?.classList.toggle('rotate-180', !isOpen);
  mobileTreatmentsBtn?.setAttribute('aria-expanded', String(!isOpen));
});


  const openMenu = () => {
    if (mobileMenu) {
      mobileMenu.classList.remove('hidden');
      document.body.style.overflow = 'hidden';

      setTimeout(() => {
        mobileMenu.classList.remove('opacity-0');
      }, 10);
    }
  };

  const closeMenu = () => {
    if (mobileMenu) {
      mobileMenu.classList.add('opacity-0');
      document.body.style.overflow = '';

      setTimeout(() => {
        mobileMenu.classList.add('hidden');
      }, 300);
    }
  };

  menuBtn?.addEventListener('click', openMenu);
  closeBtn?.addEventListener('click', closeMenu);
</script>
